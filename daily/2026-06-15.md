# 2026-06-15 - Daily MLsys Log

## Inference Track

### Today

- Started the LLM inference systems track.
- Decided to keep inference and GPU kernel work in the same public MLsys repository, but separate them by directory, filename prefix, and branch/worktree.
- Created the first project plan: `projects/llm-inference-benchmark-lab/`.

### Commands Run

```bash
git worktree add C:\Users\28100\.config\superpowers\worktrees\mlsys\inference-benchmark-lab -b codex/inference-benchmark-lab origin/main
```

### Key Outputs

```text
Inference thread owns projects/llm-inference-benchmark-lab, notes/llm-inference-*.md, and experiments/inference-*.md.
Kernel thread owns projects/gpu-mode-* and experiments/vectorsum-*.md.
pink: 2 x RTX 4090 idle, NVIDIA driver 580.142, CUDA 13.0 reported by nvidia-smi, Docker 29.1.5, Python 3.12.2.
```

### Concepts I Can Explain

- Why inference serving is measured through latency, throughput, and memory together.
- Why KV cache and batching are central to LLM inference systems.
- Why the inference and kernel tracks belong in the same MLsys repo but need file ownership rules.

### Confusions / Mistakes

- PowerShell does not support Bash-style `< script.sh` redirection, so use `Get-Content -Raw ... | ssh pink bash -s`.
- Windows CRLF line endings can break shell scripts piped to Linux bash; `.gitattributes` now forces `*.sh` to LF.
- Need to choose whether to install vLLM directly on `pink` or run it in a Docker container.

### Next Step

- Install or activate vLLM.
- Start the vLLM server with `TENSOR_PARALLEL_SIZE=1`.
- Run the one-request smoke benchmark and paste the summary into `experiments/inference-vllm-baseline-pink.md`.

## Kernel Track

- No kernel-track work in this thread.
